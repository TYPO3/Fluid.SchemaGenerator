language: php

sudo: false

php:
  - "5.5"
  - "5.6"
  - "7"
  - "7.1"
  - "hhvm"

install: composer install

script:
  - mkdir -p build/logs
  - phpunit --coverage-clover=build/logs/clover.xml

after_script:
  - vendor/bin/coveralls
